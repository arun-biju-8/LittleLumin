[pytest]
minversion = 9.0
markers =
    api: Tests targeting FastAPI backend endpoints using requests
    ui: Browser UI tests targeting web frontends using headless Selenium Chrome
    slow: Tests that take longer than usual (e.g. LLM/AI generation endpoints)
addopts = -v --html=report.html --self-contained-html
